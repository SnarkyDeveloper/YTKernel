[package]
name = "kernel"
version = "0.1.0"
edition = "2024"

[dependencies]
heapless = "0.9.3"
itoa = "1.0.18"
minipng = { version = "0.1", default-features = false }

[profile.release]
panic = "abort"
lto = true # link-time optimization
